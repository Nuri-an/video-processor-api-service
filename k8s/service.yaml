apiVersion: v1
kind: Service
metadata:
  name: video-processor-api
  namespace: video-processor
spec:
  selector:
    app: video-processor-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP